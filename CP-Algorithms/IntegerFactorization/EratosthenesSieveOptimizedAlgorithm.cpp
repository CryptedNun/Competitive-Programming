// ! The goal of the sieve() function is to PRE-COMPUTE all the prime numbers upto some number, namely the highest possible input value
// ! DO NOT use that function to compute primes AT RUN TIME, that is not efficient, rather use sieving till root

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long d; cin>>d;

}