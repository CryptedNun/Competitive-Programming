#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin>>t;
    while(t--) {
        int n, m, x, y; cin>>n>>m>>x>>y;
        for(int i=0; i<n; i++) {int dummy; cin>>dummy;}
        for(int i=0; i<m; i++) {int dummy; cin>>dummy;}

        cout<<n+m<<endl;
    }
}