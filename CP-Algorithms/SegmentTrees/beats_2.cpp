// ? Given an array 
// ? you can do (min= x) operation on a segment [l, r] and return a segment's sum

#include <bits/stdc++.h>
#define endl 'n'
using namespace std;

class SegmentTree {
    
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    
}