#include <bits/stdc++.h>
using namespace std;

class SegmentTree {

};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, q; cin >> n >> q;
    vector<int> x(n);
    for(auto& e : x) cin >> e;

    return 0;
}